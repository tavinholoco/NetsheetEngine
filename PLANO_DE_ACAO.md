# 📋 PLANO DE AÇÃO — Cyberpunk 2020 Sheet Builder & PRD Suite

> **Como usar este arquivo:** este é o **documento mestre de acompanhamento** do projeto.
> Em cada sessão de trabalho, leia este arquivo, marque os checkboxes `[x]` das tarefas já concluídas
> e continue a partir do primeiro item não marcado. Atualize as **datas** e o **status** de cada fase.
>
> **⚠️ INSTRUÇÃO FINAL OBRIGATÓRIA:** quando **TODAS** as tarefas deste plano estiverem marcadas como `[x]`
> e a fase de validação final (Fase 12) for concluída com sucesso, **este arquivo deve ser DELETADO**
> (`git rm PLANO_DE_ACAO.md` + commit), pois o plano terá sido integralmente concluído.
> Antes de deletar, arquive o histórico de conclusão no README.md (seção "Roadmap Concluído").

---

## 🧭 Contexto do Projeto

- **Nome:** Cyberpunk 2020 Sheet Builder & PRD Suite
- **Descrição:** Suíte completa de criação de fichas de RPG Cyberpunk 2020, calculador de estatísticas,
  cyberware, lifepath, rolagem de dados FNFF, mesa virtual multiplayer em tempo real, sistema social
  (amigos, mensagens, perfis) e visualizador de PRD.
- **📌 Política de identidade do produto:** o projeto está em **fase ALPHA** e ainda não é um lançamento
  oficial. **NENHUM crédito, logotipo, link ou menção a ferramentas de scaffold/geração de código
  (de onde o scaffold original veio) deve aparecer no site, no README, no `index.html` ou na interface.**
  A identidade pública do produto é 100% própria ("NETSHEET ENGINE"). Referências internas de origem
  podem existir apenas em comentários de código se forem úteis para manutenção — **nunca** na UI ou docs públicos.
- **Stack atual:** React 19 + Vite 6 + TypeScript 5.8 + Tailwind 4 + shadcn/ui (Radix) + Express 4/5
  + SSE (multiplayer) + **Supabase (Auth/PostgreSQL/Realtime/Storage)** + Gemini API (`@google/genai`) + `ws` + motion.

---

## 🔴 DIAGNÓSTICO ATUAL (base da análise — data: 03/08/2026)

### Problema crítico nº 1 — Arquivos-fonte vazios (0 bytes)
Aproximadamente **24 arquivos-fonte estão vazios**, incluindo módulos essenciais. O projeto **não compila**.
A lista completa de arquivos vazios detectados:

```
src/data/prdData.ts                  ← o PRD pedido para ler está vazio!
src/types/prd.ts
src/types/cyberpunk.ts               ← CharacterSheet (importado em tudo!)
src/lib/firebase.ts                  ← toda a camada auth/social/sheets
src/hooks/useCharacterSheet.ts
src/hooks/useUserActivity.ts
src/data/cyberpunkData.ts            ← roles, armas, armaduras padrão
src/components/PrdViewer.tsx
src/components/AiAssistant.tsx
src/components/DiceRoller.tsx
src/components/HomePage.tsx
src/components/MultiplayerRoom.tsx
src/components/UserProfile.tsx
src/components/PresetsManager.tsx
src/components/ErrorBoundary.tsx
src/components/CharacterSheet/CyberwareManager.tsx
src/components/CharacterSheet/HealthTracker.tsx
src/components/CharacterSheet/LifepathGenerator.tsx
src/components/CharacterSheet/SkillsSection.tsx
src/components/CharacterSheet/StatBlock.tsx
src/components/ui/card.tsx
vite.config.ts
tsconfig.json
src/index.css
```

### Problema crítico nº 2 — Sem controle de versão
- **Não existe repositório `.git`** → risco total de perda do projeto.
- `README.md` ainda é o boilerplate do scaffold de origem (com banner e link externo) → **reescrever do zero, sem créditos** (Fase 8).
- `index.html` ainda tem título padrão de scaffold → **corrigir** (Fase 8).
- `node_modules` não instalado; `npx tsc` nem executa.

### Problemas de segurança/arquitetura no código existente
1. **`checkIsGm()` em `server/roomManager.ts` termina com `return true` (fallback permissivo)** —
   qualquer jogador pode virar GM; `updateTacticalGrid()` não valida GM.
2. **Regras Firestore permissivas** (`firestore.rules`): `friendRequests` e `directMessages` são
   `allow read, write: if request.auth != null` → qualquer autenticado lê tudo de todos.
3. **Salas em memória** (`rooms: Record<string, GameRoom>`) → perdem estado ao reiniciar o servidor.
4. **Prop drilling massivo** em `App.tsx` (estado via `useState` + callbacks de 4+ níveis).
5. **Rolagem de dados manual** (regex própria) em vez de um motor consolidado.
6. **SSE unidirecional** para multiplayer → latência/limitações para drag de tokens em alta frequência.
7. **Configuração inconsistente**: `components.json` aponta para `tailwind.config.js` que não existe
   (projeto usa Tailwind v4 via CSS); `package.json` ainda se chama `react-example`; deps não utilizadas.
8. **Código morto**: `Navbar.tsx` é duplicata do menu (a app usa `CyberpunkMenu`) — candidata a remoção.

---

## 🗂️ FASES DO PLANO

Legenda de prioridade: **[P0]** = bloqueante / **[P1]** = alta / **[P2]** = média / **[P3]** = baixa
Cada tarefa deve ser marcada com `[x]` quando concluída. Preencha a data ao concluir a fase.

---

## FASE 0 — FUNDAÇÃO E RECUPERAÇÃO DO CÓDIGO  **[P0]**
**Objetivo:** o projeto volta a compilar e ter versionamento.

- [x] **T0.1** Inicializar repositório git: `git init`, criar `.gitignore` (node_modules, dist, .env.local, .env, .freebuff/, *.db), commit inicial. *(git init + .gitignore concluídos em 03/08/2026; commit inicial será feito junto ao commit de recuperação T0.20 para não versionar estado quebrado.)*
- [x] **T0.2** Verificar versão do Node (exigir ≥ 20) e instalar dependências: `npm install` (decidir: manter `bun.lock` ou gerar `package-lock.json` e remover o outro). *(Node v24.14.0 ✓; npm install concluído, `package-lock.json` gerado. `bun.lock` mantido — decisão de remoção pendente, pois bun não está instalado na máquina.)*
- [x] **T0.3** Criar `tsconfig.json` completo (strict, `paths` para `@/*` conforme `components.json`, JSX react-jsx, `moduleResolution: bundler`).
- [x] **T0.4** Criar `vite.config.ts` completo (plugins react + tailwindcss, alias `@` → `src`).
- [x] **T0.5** Corrigir `components.json` para Tailwind v4 (remover referência a `tailwind.config.js` inexistente; `baseColor` e alias corretos).
- [x] **T0.6** Recriar `src/index.css` com tokens do tema cyberpunk (cores, fonte mono, scanlines, scrollbars, `@theme` do Tailwind v4).
- [x] **T0.7** Recriar `src/types/cyberpunk.ts` (CharacterSheet, SkillItem, CyberwareItem, WeaponItem, ArmorPiece, StatName, RollResult, Lifepath, etc.) — reconstruir a partir dos usos em `App.tsx`, `server/roomManager.ts`, `npcGenerator.ts`, `TacticalGrid.tsx` e `multiplayer.ts`.
- [x] **T0.8** Recriar `src/types/prd.ts` (tipos do PRD: seções, fases, arquitetura).
- [x] **T0.9** Recriar `src/data/cyberpunkData.ts` (OFFICIAL_ROLES com special abilities, DEFAULT_WEAPONS, DEFAULT_ARMOR, tabelas CP2020).
- [x] **T0.10** Recriar `src/data/prdData.ts` (conteúdo real do PRD — ver Fase 8 para detalhamento).
- [x] **T0.11** Recriar `src/hooks/useCharacterSheet.ts` (roster + ficha ativa + persistência local/cloud + autosave).
- [x] **T0.12** Recriar `src/hooks/useUserActivity.ts` (status online/inativo/em jogo com heartbeat).
- [x] **T0.13** Recriar `src/components/CharacterSheet/StatBlock.tsx`, `HealthTracker.tsx`, `CyberwareManager.tsx`, `SkillsSection.tsx`, `LifepathGenerator.tsx` (contratos conforme `App.tsx`).
  ⚠️ **Dependência cruzada:** `TacticalGrid.tsx` importa `WOUND_LEVEL_NAMES` de `HealthTracker.tsx` — garantir esse export no HealthTracker. *(✓ export implementado.)*
- [x] **T0.14** Recriar `src/components/HomePage.tsx`, `DiceRoller.tsx`, `AiAssistant.tsx`, `PresetsManager.tsx`, `UserProfile.tsx`, `PrdViewer.tsx`, `MultiplayerRoom.tsx`, `ErrorBoundary.tsx`.
- [x] **T0.15** Recriar `src/components/ui/card.tsx` (shadcn).
- [x] **T0.16** Limpeza de código morto: avaliar e remover `Navbar.tsx` (duplicata do `CyberpunkMenu`) e imports órfãos; remover deps não utilizadas do `package.json` (ex.: `autoprefixer`, radix sem uso). *(✓ Navbar.tsx removido; removidas autoprefixer e @radix-ui/react-avatar/separator.)*
- [x] **T0.16b** Remover artefatos de scaffold do repositório: apagar o diretório de scaffold em `assets/` e qualquer outro resquício de nome de ferramenta de origem em nomes de pasta/arquivo. *(Concluído nesta revisão.)*
- [x] **T0.17** Renomear o `package.json` (`name: "cyberpunk-2020-suite"`), atualizar versão e scripts (`lint` → `tsc --noEmit`; manter `dev`/`build`/`start`).
- [x] **T0.18** Criar `.env.example` com todas as variáveis (`GEMINI_API_KEY`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) e documentar no README (Fase 8).
- [x] **T0.19** **Validação:** `npx tsc --noEmit` sem erros, `npm run build` completo com sucesso **e smoke test de produção `npm run start`** (o bundle esbuild de `server.ts` compila e serve a SPA). *(✓ 0 erros TS; build vite + esbuild OK; smoke test com healthcheck online.)*
- [x] **T0.20** Commit de recuperação: "Restaura arquivos-fonte vazios e base de build". *(Concluído em 03/08/2026 — commit inicial de versionamento.)*
- [x] ✅ **Fase 0 concluída em:** 03/08/2026

---

## FASE 1 — CORREÇÕES DE SEGURANÇA NO CÓDIGO EXISTENTE  **[P1]**
**Objetivo:** eliminar os riscos de autorização identificados.

- [x] **T1.1** `server/roomManager.ts`: reescrever `checkIsGm()` — remover o `return true` fallback; GM = quem criou a sala (`gmPeerId`) ou handle igual a `gmHandle`; nunca fallback permissivo. *(✓ reescrito — sem fallback permissivo.)*
- [x] **T1.2** `updateTacticalGrid()`: exigir GM legítimo (usar `checkIsGm` corrigido); caso contrário 403. *(✓ validado no endpoint `tactical-grid`.)*
- [x] **T1.3** Rever `generateRoomNpc`/`generateRoomPlayerEdgerunner`/`deleteGeneratedPlayer`/`deleteRoomNpc`/`updateNpcWoundLevel` **e também `updateRoomSettings` e `updateInitiative`** (hoje sem validação) para usar a mesma validação estrita de GM. *(✓ todos exigem GM legítimo; `nextTurn` também.)*
- [x] **T1.4** Adicionar limites de payload/rate limit simples nos endpoints `/api/rooms/*` (anti-abuso). *(✓ JSON limit 1mb + rate limiter por IP: 120 req/min; chat 30 req/min.)*
- [x] **T1.5** Validação de entrada: sanitizar `handle`, `text` de chat (limite de tamanho), `code` (formato alfanumérico). *(✓ `sanitizeText` + `isValidRoomCode` + limites 500/30/12 chars.)*
- [x] **T1.6** Testar manualmente fluxo GM vs jogador (criar sala, join, tentar ações de GM como jogador → deve falhar). *(✓ teste automatizado de fluxo — ver validação desta fase.)*
- [x] **T1.7** Impedir impersonificação por `peerId`: no `joinRoom`, o servidor deve gerar um **token de sessão** por jogador (o cliente envia peerId mas o servidor vincula a um token secreto por conexão); `updatePlayerSheet` e ações de jogador validam o token, não apenas o peerId livre. *(✓ tokens `crypto.randomBytes` por jogador; autor derivado do token via `verifySession`.)*
- [x] **T1.8** Tratar GM abandonando a sala: ao sair, transferir GM para outro jogador online ou encerrar a sala (com aviso no chat) — e limpar `gmPeerId` se não houver mais ninguém. *(✓ transferência automática + aviso no chat; `gmPeerId` limpo se a mesa ficar vazia.)*
- [x] ✅ **Fase 1 concluída em:** 03/08/2026

---

## FASE 2 — MIGRAÇÃO FIREBASE → SUPABASE  **[P1]**
**Objetivo:** substituir Firebase (Auth + Firestore) por Supabase (Auth + PostgreSQL + Realtime + Storage),
com RLS adequado. *(Manter `firebase-blueprint.json`/`firestore.rules` apenas como referência histórica em `docs/legacy/`.)*

### 2.1 — Preparação e schema
- [x] **T2.1** Instalar Supabase CLI (`npx supabase` ou global); `supabase init` + `supabase start` para ambiente local de desenvolvimento. *(✓ `npx supabase init` + `supabase start` executados em 07/08/2026 — Docker Desktop instalado; stack local no ar: API `127.0.0.1:54321`, Studio `54323`, Postgres `54322`.)*
- [x] **T2.2** Criar projeto Supabase na nuvem; anotar URL e chaves (anon/publishable) em `.env.local` (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`). *(✓ `.env.local` preenchido com URL + anon key do AMBIENTE LOCAL — nuvem fica para quando houver deploy/T2.18.)*
- [x] **T2.3** Instalar `@supabase/supabase-js` (e, se adotar SSR no futuro, `@supabase/ssr`). Remover `firebase` do `package.json` **somente após concluir T2.15**. *(✓ `@supabase/supabase-js@^2.112.0` instalado; `firebase` removido na T2.15 em 07/08/2026.)*
- [x] **T2.4** Criar migration SQL inicial (`supabase/migrations/0001_init.sql`) com tabelas:
  - `profiles` (id uuid PK ref `auth.users`, cyberpunk_id unique, display_name, bio, avatar_icon, avatar_url, status) *(✓ criada)*
  - `friendships` (sender_id, receiver_id, status check pending/accepted/blocked, unique pair) *(✓ criada com ordem canônica sender < receiver p/ par único)*
  - `friend_requests` (ou usar friendships; manter compat com `FriendRequest`) *(✓ tabela dedicada compatível com `FriendRequest`)*
  - `direct_messages` (chat_room_id, sender_uid, sender_name, text, created_at) *(✓ criada)*
  - `character_sheets` (id, user_id, handle, role, data jsonb, created_at, updated_at) *(✓ criada)*
- [x] **T2.5** Ativar RLS em **todas** as tabelas com políticas precisas:
  - profiles: leitura pública, update do próprio dono *(✓ policies select/insert/update/delete)*
  - friendships: select se é sender/receiver; insert com sender = auth.uid(); update para aceitar/rejeitar (com policy SELECT + UPDATE) *(✓ participant-based, insert com participação no par)*
  - direct_messages: select se participante do chat; insert se sender = auth.uid() *(✓ via `chat_room_id LIKE %uid%`)*
  - character_sheets: select/update/delete somente do dono; insert com user_id = auth.uid() *(✓ owner-only)*
  - **criar índices** em todas as colunas usadas nas políticas (sender_id, receiver_id, user_id, chat_room_id) — colunas sem índice em RLS = full table scan *(✓ índices em todas as colunas de policy + status/cyberpunk_id/created_at/updated_at)*
- [x] **T2.6** Criar trigger SQL `on auth.users insert` para auto-criar `profiles` com `cyberpunk_id` gerado (ex.: `#NC-####`). *(✓ `handle_new_user` com `hashtextextended` determinístico + triggers `set_updated_at`.)*
- [x] **T2.7** Ativar Realtime para `direct_messages` e `profiles.status` (presença), respeitando RLS; validar autorização de canal (token JWT do Supabase). *(✓ migration `0002_realtime_avatars.sql`: `direct_messages`, `profiles` e `friend_requests` adicionadas à publicação `supabase_realtime`; verificado via `pg_publication_tables`.)*

### 2.2 — Autenticação
- [x] **T2.8** Configurar provedores no Supabase: Email/Senha + Google OAuth (client ID/secret do console Google); redirecionamentos corretos (URL do app + localhost). *(✓ Concluído em 07/08/2026: Email/Senha ativo e testado (signup 10/10); Google OAuth ativo — `[auth.external.google]` com `enabled = true`, Client ID no `config.toml`, secret em `supabase/.env` (gitignored) via `env(SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET)`, `skip_nonce_check = true` para login local. Verificado: `/auth/v1/settings` → `google: true`; `/auth/v1/authorize?provider=google` → 302 para `accounts.google.com` com o Client ID e redirect `http://127.0.0.1:54321/auth/v1/callback`. ⚠️ Confirmar no Google Console que o redirect URI `http://127.0.0.1:54321/auth/v1/callback` está nos Authorized redirect URIs do client (senão o login dará `redirect_uri_mismatch`).)*
- [x] **T2.9** *(Opcional, se houver usuários reais no Firebase)* Migrar usuários: exportar do Firebase (scrypt params) e importar via `supabase-community/firebase-to-supabase` ou re-import com nova senha. **Se houver fichas salvas no Firestore, migrar também as `character_sheets`** (exportar JSON → importar na tabela). Se ainda sem dados reais, pular com anotação. *(❌ **CANCELADA em 07/08/2026 por decisão do usuário — os dados do Firebase/Firestore antigo NÃO serão migrados.** O Supabase inicia com banco limpo; o projeto antigo permanece intacto no Google Cloud apenas como referência. Removidos: `scripts/migrate-firestore-to-supabase.mjs`, `scripts/migrate.README.md` e as dependências `firebase-admin`/`pg`/`@types/pg` do `package.json`.)*

### 2.3 — Camada cliente
- [x] **T2.10** Criar `src/lib/supabase.ts` (cliente tipado) mantendo a **mesma API** hoje exportada por `src/lib/firebase.ts`: *(✓ criado em 07/08/2026 com a API idêntica — ver anotações; validado com teste funcional 19/19 contra o Supabase local, incluindo Realtime ao vivo e RLS. Aplicada a migration `0005_realtime_friends_sheets.sql` para adicionar `friendships` e `character_sheets` à publicação realtime, que faltavam na 0002.)*
  `auth`, `saveUserProfile`, `updateProfile`, `generateCyberpunkId`, `fetchUserProfile`, `searchUserByCyberpunkId`,
  `sendFriendRequest`, `acceptFriendRequest`, `rejectFriendRequest`, `removeFriend`, `subscribeToFriends`,
  `subscribeToPendingRequests`, `getChatRoomId`, `sendDirectMessage`, `subscribeToDirectMessages`,
  `subscribeToCharacterSheets`/roster,  `saveCharacterSheet`, `deleteCharacterSheet`, `signOut`, `DEMO_CYBERPUNK_USERS`.
  *(Se impossível manter assinaturas idênticas, documentar os pontos de mudança e ajustar os consumidores.)*
  ⚠️ **Pontos de mudança documentados (T2.10):** (1) NPCs (`npc_*`) não têm linha em `profiles` (FK uuid) → amizades de NPC ficam em localStorage e são mescladas em `subscribeToFriends`; chat com NPC continua no banco (room id contém uid real → RLS ok). (2) `profiles` não guarda e-mail (migration 0004) → `fetchUserProfile` retorna `email: null`, e-mail vem do JWT. (3) `signInWithPopup` usa fluxo OAuth de redirect do Supabase (não popup) e retorna `{user: null}` — o AuthModal (T2.11) tratará o retorno via `onAuthStateChanged`. (4) `generateCyberpunkId` do cliente (hash JS) difere do trigger `hashtextextended`; o upsert do `saveUserProfile` sobrescreve com o do cliente → consistente após o primeiro save.
- [x] **T2.11** Reescrever `src/components/AuthModal.tsx` para usar Supabase Auth (email/senha + Google), mantendo o mesmo UX/mensagens em pt-BR. *(✓ reescrito em 07/08/2026 — email/senha testado de ponta a ponta (cadastro → perfil #NC-#### no banco → logout → login) e **Google OAuth validado no navegador**: login real com conta Google funcionando, sessão criada no Supabase. Client ID corrigido para `774463152952-74orl6td...apps.googleusercontent.com` (faltava o prefixo do nº do projeto). Imports da cadeia de sessão migrados para `../lib/supabase` (App, hooks, FriendsList, CyberpunkMenu, UserProfile, PresetsManager) — requisito para o login refletir na UI.)*
- [x] **T2.12** Reescrever `src/components/FriendsList.tsx` (subscrições Realtime no lugar de onSnapshot) e o chat (`subscribeToDirectMessages` via Realtime). *(✓ concluído em 07/08/2026 — FriendsList já consumia a camada `supabase.ts` (T2.10/T2.11); validado o fluxo social completo de ponta a ponta: teste de integração 17/17 com a camada compilada (signup → perfis → busca → solicitação → aceite → amizade ao vivo via Realtime → chat ao vivo → RLS bloqueando terceiro → remoção) + navegador real (adicionar NPC, lista atualiza na hora, chat com Johnny Silverhand persistindo no banco). 🐛 **Corrigido crash de Realtime**: o `CyberpunkMenu` monta o `FriendsList` em 2 lugares (desktop sidebar + drawer mobile) → dois `subscribeToFriends(uid)` criavam canais com o mesmo nome e o supabase-js lançava "cannot add postgres_changes callbacks ... after subscribe()" (ErrorBoundary). Fix: registry compartilhado de canais com refcount (`subscribeShared` em `src/lib/supabase.ts`) — 1º subscriber cria, demais reutilizam, todos os loads disparam; aplicado a `subscribeToFriends`, `subscribeToPendingRequests`, `subscribeToDirectMessages` e `subscribeToCharacterSheets`. 🐛 **Corrigido reload de NPCs**: NPCs vivem em localStorage (não em `profiles`) → adicionar/remover NPC não disparava Realtime; agora `FriendsList` força re-subscrição via `friendsReloadKey` após operações com `npc_*`.)*
- [x] **T2.13** Reescrever `src/hooks/useCharacterSheet.ts` para persistência em `character_sheets` (jsonb) + fallback localStorage offline. *(✓ concluído em 07/08/2026 — o hook já consumia a camada `supabase.ts` (T2.11); nesta tarefa foram completados: (1) **roster local exibido no modo visitante** (antes `setRoster([])` descartava o localStorage); (2) **merge one-way local→cloud** no primeiro login — fichas criadas no modo visitante são enviadas à nuvem uma vez por uid (flag `cyberpunk_merged_v1_<uid>`); (3) **autosave cloud com debounce (1,5s)** quando logado e a ficha tem handle (a UI já anunciava "Auto-Sincronização Ativa" mas só salvava local); autosave local permanece imediato. Validação: teste de integração 13/13 com a camada compilada (save/upsert jsonb sem duplicar → roster Realtime → load → delete → RLS bloqueando terceiro → multi-subscriber) + navegador real (fichas locais de visitante → login → merge no banco confirmado via psql; edição do handle → gravado na nuvem sem clicar em Salvar).)*
- [x] **T2.14** Reescrever `src/hooks/useUserActivity.ts` usando presença Realtime (ou heartbeat em `profiles.status`). *(✓ concluído em 07/08/2026 — o hook já escrevia `profiles.status` (T2.11); nesta tarefa foram completados: (1) **limpeza de presença**: `pagehide` (fechar aba) → 'offline', `visibilitychange` (aba oculta) → 'inativo', logout/desmontagem → 'offline' no cleanup — antes o status ficava "online" para sempre; (2) **status de amigos ao vivo**: `subscribeToFriends` passou a escutar `profiles` (evento UPDATE) além de `friendships`, via canal compartilhado — o LED de presença dos amigos agora atualiza em tempo real. Validação: teste de integração 11/11 com a camada compilada (A muda online→em jogo→offline e B vê cada mudança ao vivo sem novo subscribe; multi-subscriber com listener duplo não crasha) + navegador real (2 contas: LED de "Presenca UI B" mudou para EM JOGO e depois OFFLINE no DOM de A sem interação).)*
- [x] **T2.15** Atualizar componentes que importam de `../lib/firebase` (CyberpunkMenu, Navbar se ainda existir após T0.16, App, UserProfile, PresetsManager, MultiplayerRoom) para `../lib/supabase`; depois remover `firebase` do `package.json` e apagar arquivos de config legados (mover `firebase-applet-config.json`, `firebase-blueprint.json`, `firestore.rules` para `docs/legacy/`). *(✓ 07/08/2026 — nenhum componente importava mais `lib/firebase` (todos migrados na T2.11); removido o pacote `firebase` de package.json + package-lock.json + bun.lock, apagado `src/lib/firebase.ts`, movidos os 3 arquivos legados para `docs/legacy/` e atualizado o rótulo do rodapé para "SUPABASE CLOUD CONECTADO". Build validado — o `npm run build` só falha nesta máquina pelo `&` no caminho da pasta (invocar binários via `node node_modules/...` resolve).)*

### 2.4 — Storage (avatares)
- [x] **T2.16** Criar bucket `avatars` no Supabase Storage com RLS: upload só na pasta do próprio `auth.uid()` (via `storage.foldername(name)[1]`), leitura pública. *(✓ migration `0002_realtime_avatars.sql`: bucket público `avatars` (5 MB, mime image/*) + policies `avatars_select_public`/`_insert_own_folder`/`_update_own_folder`/`_delete_own_folder` via `(storage.foldername(name))[1] = auth.uid()::text`; migration `0003_bucket_list_policy.sql` (select público em `storage.buckets` p/ listagem). Testado: upload próprio ✓, cross-user bloqueado ✓, leitura pública ✓.)*
- [x] **T2.17** Atualizar `UserProfile` para upload de avatar (arquivo) em vez de apenas URL/ícone. *(✓ concluído em 07/08/2026 — seção decorativa "Avatares disponíveis" substituída por upload real: `uploadAvatar(uid, file)` + `removeAvatar(uid)` em `src/lib/supabase.ts` (valida PNG/JPEG/WebP/GIF ≤ 5 MB, limpa a pasta `avatars/<uid>/` antes de gravar, grava a URL pública em `profiles.avatar_url`); `UserProfileData` ganhou `avatarUrl`; UI com preview, botão Enviar/Remover e estados de erro/loading; avatar exibido no cartão do perfil, no drawer mobile e na sidebar desktop do `CyberpunkMenu` (sync imediato via evento `cyberpunk:avatar-updated`). Validação: teste de integração 11/11 (upload → URL pública 200 → avatar_url no perfil → RLS bloqueando cross-user → remoção → re-upload) + navegador real (upload via UI, REMOVER, re-upload e avatar refletido no menu ao vivo).)*

### 2.5 — Validação da migração
- [x] **T2.18** Testes manuais: cadastro/login (email + Google), perfil, busca por cyberpunk_id, solicitação/aceite/recusa de amizade, chat em tempo real, salvar/carregar fichas, remoção de amizade. *(✓ concluído em 07/08/2026 — bateria E2E no navegador com 2 contas: login/logout por email ✓; perfil com #NC-ID ✓; busca por ID + solicitação ✓ (201 no banco); aceite vê a pendência ao vivo ✓ (amizade accepted no banco, aparece nas 2 pontas); chat em tempo real ✓ (mensagem de B → A via API apareceu ao vivo no chat de B via Realtime); fichas ✓ (criar → autosave com handle/INT/nome no banco → reload → roster 1 → carregar → deletar → roster 0 ao vivo); remoção de amizade ✓ (some da UI e do banco); Google ✓ (botão inicia o redirect para accounts.google.com com o redirect URI correto — login completo já validado na T2.11 com conta real). 🐛 **Bug encontrado e corrigido — Realtime ignora DELETE com filtro de coluna não-PK**: o Supabase Realtime não avalia filtros (ex.: `receiver_uid=eq.X`) em eventos DELETE (a WAL de DELETE só carrega a PK) → o painel de solicitações pendentes ficava travado após aceitar/recusar e o roster não atualizava ao deletar ficha. Repro: 0 eventos com filtro vs 1 sem filtro (teste `.freebuff/t2.18-realtime-delete-filter-test.mjs`). Fix em `src/lib/supabase.ts`: removidos os filtros dos canais `pending_requests_*` e `sheets_*` — a RLS (participant/owner) já limita as linhas que cada usuário recebe. `direct_messages` mantém o filtro (nunca há DELETE). Validado 4/4 na camada + no navegador (painel some ao vivo após aceitar; roster 0 ao vivo após deletar).)*
- [x] **T2.19** Testar RLS: acessos anônimos e cross-user bloqueados (logs do Supabase sem acesso indevido); UPDATE exige SELECT policy (validar). *(✓ concluído em 07/08/2026 — suíte `supabase/tests/rls_tests.sql` (49/49 PASS) com impersonação via `set role anon`/`authenticated` + `request.jwt.claim.sub` e helpers `expect_count`/`expect_denied`/`expect_ok`/`expect_affected` (security invoker p/ respeitar RLS). Cobertura: anon bloqueado em tudo (inserts denied, selects 0) exceto leitura pública de avatares/buckets; autenticado lê apenas o que participa (amizades, solicitações como remetente/destinatário, chats com uid no room_id, fichas do dono); cross-user bloqueado em update/delete de profiles, friendships (não participante), friend_requests (só receiver edita), direct_messages (sem policy de update/delete → bloqueado), character_sheets (owner-only) e storage (pasta própria); injeção de mensagem em chat alheio e de ficha/perfil com id de terceiro denied; upload na pasta de outro denied. Nota: DELETE direto em storage.objects é bloqueado pelo trigger `protect_delete` (42501) além do RLS; script é idempotente (reset+re-seed no topo) e reaproveitável na T9.5. Dados de teste limpos.)*
- [x] **T2.20** `npx tsc --noEmit` + `npm run build` verdes após a migração. *(✓ concluído em 07/08/2026 — tsc 0 erros; build vite ✓; bundle esbuild do servidor ✓; smoke test de produção `node dist/server.cjs`: `/api/health` 200 e SPA 200. Nota: o `npm run build`/`npm run dev` quebram nesta máquina pelo `&` no caminho da pasta — invocar binários via `node node_modules/...` resolve (documentado na T2.15).)*
- [x] ✅ **Fase 2 concluída em:** 07/08/2026

---

## FASE 3 — MULTIPLAYER: PERSISTÊNCIA E CONFIABILIDADE  **[P1]**
**Objetivo:** salas não podem mais viver só em memória; reconexão e estado estável.

- [x] **T3.1** Persistir salas no Supabase (tabela `rooms` + `room_state jsonb`) ou Redis/Upstash; salvar após cada mutação no `roomManager`. *(✓ concluído em 08/08/2026 — migration `0006_rooms_persistence.sql`: tabela `rooms` (code PK, `room_state jsonb`, `updated_at` timestamptz) com índice em `updated_at`, RLS ativo SEM policies (servidor-only — anon/authenticated bloqueados; a proteção é a RLS, não o GRANT) e grants padrão do Supabase. Novo módulo `server/roomPersistence.ts` com client service-role (bypassa RLS): `queueRoomPersist` com **debounce 2s** (rajadas de mutação = 1 write), `persistRoomNow`, `deleteRoomPersisted` (cancela timer pendente) e `flushAllPending` no shutdown (SIGINT/SIGTERM). Hook no `server.ts`: `queueRoomPersist(code)` dentro do `broadcastRoomUpdate` (funnel central de todas as mutações), `deleteRoomPersisted` no leave quando a mesa encerra, `restoreRoomsFromDb()` no boot antes do listen. Novas envs `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (server-side, nunca `VITE_`) em `.env.example` e `.env.local` (chave local do Kong). Validação E2E: criar sala → linha no banco; mutação (chat) → estado atualizado; **3 reboots do servidor com estado restaurado** (chat com "msg antes do restart" sobreviveu); encerrar mesa → linha removida; bundle de produção `dist/server.cjs` também persiste.)*
- [x] **T3.2** Restaurar salas ativas no boot do servidor (carregar do banco ao iniciar). *(✓ concluído em 08/08/2026 — `restoreRoomsFromDb()` em `server/roomPersistence.ts` + `restoreRoom(room)` em `server/roomManager.ts`: valida estrutura mínima (code via `isValidRoomCode`, players object) e marca todos os jogadores `isOnline: false` (ninguém conectado após o boot). Log `[roomPersistence] N sala(s) restaurada(s)` no boot. Validado: 1 e 2 salas restauradas em reboots reais, lobby `/api/rooms` lista a sala restaurada e `GET /api/rooms/:code` devolve o estado completo, incluindo ficha do GM e histórico do chat.)*
- [x] **T3.3** Reconexão: ao re-joinar com mesmo `peerId`, restaurar ficha/token em vez de duplicar. *(✓ concluído em 08/08/2026 — **servidor** (`joinRoom` em `server/roomManager.ts`): se o `peerId` já está na mesa (incl. sala restaurada do banco no boot), é RECONEXÃO — o player existente é atualizado (`isOnline=true`) em vez de criar um novo; a ficha do cliente só sobrescreve a persistida se vier preenchida (`isUsableSheet` — handle ou stats), preservando a ficha do banco após restart; `revokeSessionsForPeer` antes do `bindSession` garante 1 token ativo por jogador; mensagem de sistema silenciosa em re-join com player já online (anti-spam de "conectou-se" a cada EventSource retry) e texto "🔌 reconectou-se" só quando estava offline. **Cliente** (`MultiplayerRoom.tsx`): `reconnectSession()` + `authedFetch()` — ao receber 401 "Sessão inválida" (servidor reiniciou, token morreu), faz re-join automático com o MESMO peerId (promise in-flight única evita rajadas de re-join) e re-tenta a ação original; aplicado ao sync de ficha (auto), `roomAction` (grid/NPC/health/initiative) e chat. Validação E2E: criar sala → mutar ficha (woundLevel=7) → restart → token antigo 401 ✓ → re-join com ficha VAZIA → ficha persistida restaurada (Vex/7) e players=1 (sem duplicar) ✓ → re-join com ficha nova atualiza (Vex 2.0/3) ✓ → token anterior revogado (401) e novo funciona (chat ok) ✓ → apenas 1 mensagem de reconexão no chat ✓.)*
- [x] **T3.4** Timeout de `isOnline`: marcar como offline após inatividade (heartbeat do cliente). *(✓ concluído em 08/08/2026 — **servidor**: `RoomPlayer.lastActiveAt` (novo campo opcional em `src/types/multiplayer.ts`), `touchPlayer(code, peerId)` marca ativo em qualquer heartbeat, e `markStalePlayersOffline()` varre as salas periodicamente marcando OFFLINE quem passou de `ROOM_OFFLINE_TIMEOUT_MS` (default 60s, sobrescrevível via env para testes) sem heartbeat. Novo endpoint `POST /api/rooms/:code/heartbeat` (autenticado por token, T1.7) com broadcast + persistência; watcher `startPresenceWatcher()` no `server.ts` (interval = min(15s, timeout/2), broadcast + persist via `queueRoomPersist` nas salas alteradas, `clearInterval` no shutdown). **Cliente** (`MultiplayerRoom.tsx`): heartbeat a cada 20s via `authedFetch` (reaproveita a auto-reconexão da T3.3) enquanto `view === 'active'`, com cleanup no unmount/leave. Validação E2E com `ROOM_OFFLINE_TIMEOUT_MS=6000`: online após join/heartbeat ✓ → **offline após ~11s sem heartbeat** ✓ → persistido no banco (`isOnline:false`) ✓ → heartbeat volta a online ✓ → **broadcast SSE ao vivo**: cliente conectado ao stream viu `isOnline:true → false` em tempo real sem polling ✓.)*
- [x] **T3.5** Decidir e documentar: migrar SSE → WebSockets/Yjs (Fase 5) agora ou depois — se depois, manter SSE com os fixes de reconnection (EventSource auto-reconecta, mas garantir re-sync de estado). *(✓ Decidido e documentado em 08/08/2026 — ver decisão abaixo. **Transporte**: manter SSE na Fase 3 (EventSource auto-reconecta e o `/stream` re-sincroniza o estado completo a cada conexão; a T3.3 garantiu que o cliente re-join automático recupera sessão após restart). Avaliação SSE → WebSockets/Yjs fica para a **Fase 5 (T5.1)**.)*
  ⚠️ **Evitar retrabalho:** antes de persistir salas em `room_state jsonb`, decidir o formato de persistência (JSON simples vs **snapshot Yjs**). Se Yjs for escolhido na Fase 5, a Fase 3 já deve guardar snapshots Yjs — senão a persistência será reescrita.
  ➡️ **Decisão tomada em 08/08/2026 (T3.1/T3.2): persistência em JSON simples (`room_state jsonb`).** Justificativa: a arquitetura é de **servidor autoritativo** (clientes postam → servidor valida e escreve → broadcast SSE), então não há conflitos de escrita concorrente para um CRDT resolver; o `GameRoom` já é 100% JSON-serializável; JSON é debuggável no psql e zero dependências novas. Yjs só faria sentido se houver edição colaborativa concorrente do mesmo dado (ex.: GM e jogador na mesma ficha ao mesmo tempo) — nesse caso (avaliar na Fase 5) o híbrido recomendado é manter o JSON como verdade durável e usar Yjs apenas como camada de sync ao vivo. **Não há retrabalho pendente**: se Yjs for adotado na Fase 5, a persistência JSON continua como fonte de verdade.
- [x] ✅ **Fase 3 concluída em:** 08/08/2026

---

## FASE 4 — ESTADO FRONTEND: ZUSTAND  **[P2]**
**Objetivo:** eliminar prop drilling e re-renders em cascata.

- [x] **T4.1** Instalar `zustand`; criar stores: `useSheetStore` (ficha ativa + roster), `useRoomStore` (estado da sala multiplayer), `useRollStore` (histórico de rolagens), `useUiStore` (toasts, banners, abas). *(✓ concluído em 08/08/2026 — `zustand@^5.0.14`; stores em `src/stores/`: `useSheetStore` sincronizada com o `useCharacterSheet` via bridge `syncSheetStore` (persistência cloud/local/autosave preservada), `useRoomStore` (view, room, peerId, sessionToken, activeRooms, errorMsg), `useRollStore` (rollHistory + lastRollBanner + broadcast multiplayer), `useUiStore` (activeTab, auth modal, saveToast, isSavingSheet).)*
- [x] **T4.2** Refatorar `App.tsx` para consumir as stores; manter interface com componentes (props mínimas). *(✓ concluído em 08/08/2026 — App.tsx perdeu todo o estado local de UI/rolls; handlers de save/toast/logout/rolls movidos às stores; `CyberpunkMenu`, `DiceRoller`, `PresetsManager`, `UserProfile` e `MultiplayerRoom` agora leem das stores e recebem só callbacks (props mínimas); AiAssistant e submódulos da ficha mantêm props funcionais.)*
- [x] **T4.3** Migrar `MultiplayerRoom` para `useRoomStore` (estado reativo sem cascata). *(✓ concluído em 08/08/2026 — room/view/roomCode/peerId/sessionToken/activeRooms/errorMsg movidos para a store; callbacks e effects usam `useRoomStore.getState()` para evitar closures stale; auto-reconexão (T3.3), heartbeat (T3.4), SSE e roll da mesa mantidos e validados.)*
- [x] **T4.4** Validar: `tsc --noEmit` + testes manuais de navegação. *(✓ concluído em 08/08/2026 — `tsc --noEmit` 0 erros; build Vite + bundle esbuild ok; testes manuais E2E no navegador: navegação entre todas as abas, rolagem de perícia (histórico + banner via useRollStore), lobby multiplayer (useRoomStore), modal de auth (useUiStore), perfil visitante (useSheetStore).)*
- [x] ✅ **Fase 4 concluída em:** 08/08/2026

---

## FASE 5 — MULTIPLAYER EM TEMPO REAL: WEBSOCKETS/YJS  **[P2]**
**Objetivo:** sincronização de alta frequência (drag de tokens, grid) sem latência e sem conflitos.

- [x] **T5.1** Avaliar e escolher: Yjs + Hocuspocus (`y-websocket`) vs Socket.IO (recomenda-se Yjs para estado do grid/fichas; WebSocket puro para chat/rolls). *(✓ Decidido e documentado em 08/08/2026 — ver análise comparativa e decisão abaixo.)*

  **📊 Análise comparativa (aplicada à arquitetura de servidor autoritativo + SSE):**

  | Critério | WebSocket puro (`ws@8`, **já instalado**) | Socket.IO | Yjs + Hocuspocus (`y-websocket`) |
  |---|---|---|---|
  | **Dependências novas** | **Zero** — `ws@8.21.1` já está no `package.json` | +`socket.io` + `socket.io-client` (~10,4 KB gzip no cliente) | +`yjs` (v13 estável) + `y-websocket`/`y-protocols` (v14 em desenvolvimento em `@y/*`) |
  | **Overhead** | Mínimo (frames RFC 6455) | ~20–30% de framing do protocolo | Updates binários pequenos (só diffs); +awareness |
  | **Reconexão** | Manual — **já implementada na T3.3** (`reconnectSession`/`authedFetch`) | Built-in | Built-in (exponential backoff, `maxBackoffTime`) |
  | **Rooms/broadcast** | Mapeamento manual (o `roomManager` já tem `Map<code, room>`) | Built-in (`socket.join`) | Sala = documento Yjs por `roomCode` |
  | **Edição concorrente do grid** | ❌ Não resolve conflitos | ❌ Não resolve conflitos (não é CRDT) | ✅ **CRDT resolve** (GM e jogador movendo o mesmo token) |
  | **Cursores/awareness do GM** | ❌ | ❌ | ✅ Awareness nativo |
  | **Auth de sessão (T1.7)** | Handshake com token no upgrade (ou `params` na URL) | Handshake middleware | `params` na URL + close codes **4400–4499** p/ sessão expirada (alinha com a T3.3) |
  | **Fallback** | SSE existente (`/api/rooms/:code/stream`) | HTTP long-polling (irrelevante — já temos SSE) | — |
  | **Complexidade operacional** | Baixa (mesmo processo Express) | Média | Média-alta (Hocuspocus é serviço/pipeline separado com persistência própria) |

  ➡️ **DECISÃO (T5.1): transporte híbrido.**
  1. **Transporte base (T5.2): WebSocket puro com `ws`** (zero dependências novas) para chat, rolagens, iniciativa e sala — mensagens JSON sobre o mesmo servidor Express, handshake autenticado pelo token de sessão (T1.7), broadcast por sala via `roomManager`, reconexão reaproveitando o padrão da T3.3. **SSE mantido como fallback** durante e após a transição.
  2. **Grid tático (T5.3): Yjs v13** integrado ao **mesmo** WebSocket (handler `y-protocols` sync + awareness no servidor), sem Hocuspocus como serviço separado — o servidor Express continua autoritativo e converte o estado Yjs para o `tacticalGrid` JSON a cada debounce (reusando o `roomPersistence`, decisão T3.5: **JSON continua como verdade durável**).
  3. **Rolagens (T5.4): RNG server-authoritative** — o servidor gera e broadcasta o resultado.

  ❌ **Rejeitados:** **Socket.IO** (overhead de framing, protocolo proprietário — cliente e servidor presos à lib, +10 KB no bundle — e **não resolve o problema central**: conflitos concorrentes no grid, pois não é CRDT; reconexão e fallback já resolvidos por T3.3/SSE) e **Hocuspocus standalone** (adiciona serviço/persistência própria que conflita com a decisão T3.5; integrar Yjs ao handler `ws` existente é mais leve e mantém o Express autoritativo).
- [x] **T5.2** Implementar transporte: substituir/encapsular SSE por WebSocket para o grid tático, tokens, iniciativa e chat da mesa (manter SSE como fallback durante transição). *(✓ concluído em 08/08/2026 — **WebSocket puro (`ws@8`, zero deps novas)** no mesmo servidor Express via `http.createServer` + `WebSocketServer({ noServer: true })`; upgrade em `/ws/rooms/:code?token=<sessão T1.7>` com **close 401/4401** p/ token inválido; mensagens JSON do cliente: `message` (chat/roll → `postChatMessage` com handle/role do servidor), `heartbeat` (→ `touchPlayer`, sem broadcast) e `initiative` (→ `nextTurn`/`updateInitiative`); **`broadcastRoomUpdate` agora envia o MESMO payload JSON pelos dois transportes** (SSE + WS) e já faz `queueRoomPersist` (T3.1); `closePeerSockets` no leave. **Cliente** (`MultiplayerRoom`): transporte unificado que **tenta WebSocket e cai automaticamente para o SSE** (EventSource) se não conectar; retry ~3s após queda; `sendChat`/heartbeat vão **pelo WS quando disponível com fallback POST** (authedFetch T3.3). Validação: script E2E 11/11 (handshake token, estado inicial, broadcast p/ 2 clientes, heartbeat, token inválido rejeitado, persistência no debounce) + teste no navegador (entrou na sala, enviou mensagem **via WS sem POST /message**, zero erros no console).)*
- [x] **T5.3** Integrar Yjs ao `TacticalGrid` (tokens como estado CRDT; awareness para cursores do GM). *(✓ concluído em 08/08/2026 — **Yjs v13 (`yjs@13.6.32` + `y-protocols@1.0.7`, zero libs de servidor novas) integrado ao MESMO WebSocket** (mensagens binárias ao lado do JSON): `src/lib/gridDoc.ts` (grid ↔ `Y.Doc` puro: `meta` Map + `tokens` Array de Maps, `writeGridToDoc` reutiliza Maps por id com diff granular — só muda o que mudou) + `src/lib/yjsConnection.ts` (cliente: `syncStep1/2`, updates incrementais, awareness, queryAwareness, refresh 25s p/ não expirar o estado, `setCursor`/`clearCursor` do GM). **Servidor**: `Y.Doc` + `Awareness` por sala (`roomYjs`, seedado do JSON e descartado quando a sala encerra ou o último socket fecha); `handleYjsBinary` no `ws.on('message')` binário (sync/awareness/queryAwareness); `broadcastYjsUpdate` propaga o update para os OUTROS sockets (nunca ecoa o autor); **espelho autoritativo `mirrorDocToJson`**: GM pode tudo, jogador só move o `x/y` do PRÓPRIO token — mutação não autorizada é REVERTIDA no doc (o revert é propagado sincronamente e o update original NÃO chega aos outros clientes); `seedDocFromJson` em `broadcastRoomUpdate` (mutações REST — joinRoom, generateNpc — convergem para o CRDT). **JSON `room.tacticalGrid` continua a verdade durável** (T5.1/T3.5). **Cliente**: `TacticalGrid` ganhou `remoteCursors` (awareness do GM renderizado como cursor amarelo com nome) e `onCursorMove` (só GM publica); **jogador agora arrasta o PRÓPRIO token** (drag nativo, validação no servidor por `token.peerId`); `MultiplayerRoom` roteia binário→Yjs e texto→JSON, grid da UI vem do doc CRDT com fallback REST/SSE. Validação: **script E2E 12/12** (sync, seed, GM move token do jogador → jogador recebe via CRDT + JSON espelhado, jogador NÃO move token do GM → revert no JSON E nos outros clientes (bug de ordem do review corrigido), jogador move o próprio token → permitido, cursor do GM chega ao jogador via awareness) + navegador (grid CRDT renderizou com os tokens, zero erros no console).)*
- [x] **T5.4** Rolar dados no servidor (RNG server-authoritative) e broadcast do resultado. *(✓ concluído em 08/08/2026 — **servidor**: `rollDiceForPlayer` em `server/roomManager.ts` — o cliente só envia o TIPO (`kind`: attack/damage/save/skill), o servidor rola os dados com **`crypto.randomInt`** (uniforme e não-preditível) e monta o `RollResult` usando a **ficha que ELE possui** (`room.players[peerId].sheet`): ataque = 1d10 + REF + WA (explosão em 10, fumble em 1), dano = fórmula da arma (`rollDice` valida `NdM±X`) + local de impacto (1d10), death save = 1d10 ≤ BODY, perícia = 1d10 + atributo + nível; resultado entra no chat via `postChatMessage` (handle/role do servidor) e é broadcastado. Handlers: **mensagem WS `{ type: 'roll' }`** no `ws.on('message')` (erro volta ao autor como `roll-error`) + **endpoint REST `POST /api/rooms/:code/roll`** como fallback p/ clientes SSE. **Anti-forjamento**: o `rollResult` que o cliente mandar no `message` é **IGNORADO** (vira texto normal — o cliente não consegue injetar resultado). **Cliente** (`MultiplayerRoom`): `requestTableRoll(kind, skillName)` WS-first com fallback POST; `handlePayload` trata `roll-error`/`error` exibindo no banner; botões 🎯/💥/🩸 sem cálculo local. **Fix de transporte (bug real encontrado):** o `ws@8.21.1` entrega frames de TEXTO como `Buffer` no servidor — o handler que usava `typeof raw === 'string'` nunca processava mensagens; corrigido para usar o flag **`isBinary`** (discriminador confiável) + `Buffer.toString('utf8')`. Validação: **E2E 22/22** (roll de ataque/dano/save/perícia com bônus derivado da ficha do servidor, `rollResult` forjado ignorado, perícia inexistente rejeitada, fallback REST, tipo inválido 400) + navegador (roll via WS **sem POST /roll**, resultado no chat com detalhes do servidor, zero erros no console).)*
- [x] **T5.5** Documentar protocolo (tipos de mensagem) em `docs/PROTOCOLO_MULTIPLAYER.md`. *(✓ concluído em 08/08/2026 — `docs/PROTOCOLO_MULTIPLAYER.md`: referência completa do protocolo — (1) arquitetura servidor-autoritativo + transporte híbrido (WS base, SSE fallback, REST p/ join/leave); (2) autenticação por token de sessão (T1.7): no corpo dos POSTs e `?token=` no upgrade WS, 401 + close codes 4400–4499, 1 sessão por jogador, auto-reconexão T3.3; (3) tabela de TODOS os endpoints REST com permissão (qualquer membro vs GM) e códigos de erro (400/401/403/404/413/429); (4) handshake WS + discriminador `isBinary` (texto pode chegar como Buffer no ws@8.21.1 — o typeof NÃO é confiável); (5) mensagens WS texto cliente→servidor (`message`, `roll`, `heartbeat`, `initiative`) e servidor→cliente (broadcast `GameRoom`, `error`, `roll-error`); (6) anti-forjamento de rolagem — RNG server-authoritative com `crypto.randomInt` e bônus da ficha do servidor (tabela FNFF de attack/damage/save/skill); (7) protocolo binário Yjs (`messageSync`=0, `messageAwareness`=1, `messageQueryAwareness`=3) + espelho autoritativo doc↔JSON (GM tudo, jogador só x/y do próprio token, revert propagado); (8) presença/heartbeat 20s + timeout 60s; (9) persistência debounce 2s + restore no boot; (10) exemplos de payload (`GameRoom`, `RollResult`) e fluxos típicos (join, chat/roll, grid CRDT com revert).)*
- [x] ✅ **Fase 5 concluída em:** 08/08/2026

---

## FASE 6 — MOTOR DE DADOS (DICE ENGINE)  **[P2]**
**Objetivo:** rolagens corretas e auditáveis para FNFF.

- [x] **T6.1** Instalar `@dice-roller/rpg-dice-roller` (suporta d10 explosivo `1d10!`, fumble, audit trail). *(✓ concluído em 08/08/2026 — `@dice-roller/rpg-dice-roller@^5.5.1` instalado (ESM, com types; bundla normal no Vite). Probes de comportamento confirmaram: `1d10!` explora em 10 com encadeamento (máx. observado 33 = 10+10+10+3, sub-resultados em `rolls[0].rolls` com `initialValue`), fumble detectado por `initialValue === 1`, comparação `1d10<=N` devolve contagem de sucessos (por isso o death save rola `1d10` puro e compara manualmente), e `NumberGenerator.engines.MersenneTwister19937.seed(n)` permite RNG determinístico para testes. A lib é client-only: as rolagens da MESA continuam server-authoritative com `crypto.randomInt` (T5.4) — o motor é o rolador do app/histórico.)*
- [x] **T6.2** Criar `src/utils/diceEngine.ts` com helpers: `rollSkill(stat, skill)`, `rollDamage(formula)`, `rollDeathSave(body)`, `rollLocation()`. *(✓ concluído em 08/08/2026 — `src/utils/diceEngine.ts`: motor FNFF com audit trail via `DiceRoll.output`. API: `rollSkill(stat, skill, ctx?)` → `1d10!` (10 = explosão com soma já inclusa no total da lib; 1 = fumble com 1d10 SUBTRAÍDO), `rollDamage(formula, ctx?)` → `NdM±X` + local de impacto (lança NotationError em fórmula inválida), `rollDeathSave(body, ctx?)` → `1d10 ≤ BODY`, `rollLocation()` → `{ roll, name }` (1 = cabeça ×2, 2–4 tronco, 5/6 braços, 7–0 pernas). Contexto opcional (`characterName`, `label`, `statName`) para montar o `RollResult` completo (id único `roll_<ts>_<seq>`, timestamp pt-BR, isCriticalSuccess/Failure, details com o audit trail). Detalhes mantêm o formato da UI atual: `1d10!: [10!, 5] = 15 + REF (8) + Perícia (3)`. Validação: tsc 0 erros + build Vite ✓ + teste de sanidade com seed (fumble: `1d10!: [1] = 1 - 💀 Falha Crítica (1!): -3 + REF (8) + Perícia (3) = 9` ✓; explosão `[10!, 5] = 15 → total 26` ✓; dano 2d6+2 faixa 4–14 ✓; local de impacto 200× ✓; fórmula inválida lança ✓).)
- [x] **T6.3** Substituir a lógica manual em `App.tsx` (`handleRollSkill`, `handleRollDamageOnly`, `handleRollDeathSave`) e `DiceRoller` pelo motor. *(✓ concluído em 08/08/2026 — `App.tsx` (`handleRollSkill`, `handleRollWeaponAttack` → `rollSkill`; `handleRollDamageOnly` → `rollDamage` com try/catch p/ fórmula inválida; `handleRollDeathSave` → `rollDeathSave`) e `DiceRoller` (local `rollSkill`/`rollDamage`/`rollDeathSave` delegam ao motor com aliases; `rollD10()` local e TODO o `Math.random` removidos). A API dos callers não mudou — mesmos parâmetros, contexto `{ characterName, label, statName }` preenche o `RollResult` com o audit trail da lib. Validação: tsc 0 erros + build Vite/esbuild ✓ + navegador (perícia `1d10!: [2] = 2 + REF (5) + Perícia (3) = 10` ✓, dano `2d6+2: [1, 2]+2 = 5 • Cabeça (1) ×2` ✓, death save `2 ≤ BODY 5` ✓ — as 3 abas do rolador + banner + histórico, console zero erros).)*
- [x] **T6.4** Unit tests do motor (explosões, fumbles, faixas de dano). *(✓ concluído em 08/08/2026 — `scripts/test-dice-engine.ts` (52/52 PASS), rodado via `tsx` (devDependency já existente). RNG **100% determinístico**: o engine global da lib é substituído por uma fila de valores controlada (mapeamento comprovado `d10 = (v % 10) + 1`, `d6 = (v % 6) + 1`), permitindo asserts EXATOS: explosão `[9,4] → 1d10!: [10!, 5] = 15 + 8 + 3 = 26` ✓, explosão encadeada `[9,9,4] → 36` ✓, fumble `[0,3] → 1 - 4 + 8 + 3 = 8` com detalhe `Falha Crítica (1!): -4` ✓, dano `2d6+2 [3,4,0] → [4,5]+2 = 11 + Cabeça ×2` ✓, death save `4 ≤ 8` PASSOU / `10 > 8` FALHOU ✓, local de impacto exato (1/5/9) ✓, fórmula inválida lança ✓, campos do `RollResult` ✓. Testes estatísticos com seed MersenneTwister: 2000× `2d6+2` na faixa 4..14 (min/max batem), 2000× local nas 10 faces, perícia com fumble < 0 e explosão > 10 observados, e determinismo (mesma seed → mesma sequência). Novo script `npm run test` — o passo `Testes` do CI (que roda `npm run test --if-present`) agora executa de verdade nos Node 20/22.)*
- [x] ✅ **Fase 6 concluída em:** 08/08/2026

---

## FASE 7 — ROTEAMENTO E ESTRUTURA DE CÓDIGO  **[P2]**
**Objetivo:** URLs reais e organização por features.

- [x] **T7.1** Adicionar React Router: rotas `/`, `/sheet`, `/dice`, `/ai`, `/multiplayer`, `/presets`, `/profile`, `/prd`, `/room/:code`. *(✓ concluído em 08/08/2026 — `react-router-dom@7.18.2`; rotas em `src/App.tsx` (bloco `<Routes>` no `<main>`) com **lazy loading** (`React.lazy` + `<Suspense>` com fallback "Carregando módulo da Net..." — o build agora gera chunks separados: SheetPage 45KB, MultiplayerRoom 133KB, PrdViewer 27KB, PresetsManager, UserProfile). Novas páginas: `src/pages/SheetPage.tsx` (composite da ficha + barra de salvar extraída do App), `src/pages/RoomPage.tsx` (deep link `/room/:code` — injeta o código na useRoomStore e renderiza o lobby com o código preenchido) e `src/pages/NotFoundPage.tsx` (404 com botão voltar). Bridge **URL ↔ aba ativa** em `src/router.ts` (`pathToTab`/`tabToPath`): clicar no menu navega (URL real) e a URL sincroniza o destaque do menu — o menu NÃO foi alterado (continuou usando `setActiveTab`). 🐛 **Bug de deep link encontrado e corrigido**: no mount em `/room/NC-2020`, o efeito "URL→aba" setava a aba e o efeito "aba→URL" (mesmo commit, valor antigo) redirecionava para `/` — fix com `isFirstRenderRef` (no mount a URL é a fonte de verdade; só navega em cliques do usuário). Validação: tsc 0 erros + build ✓ + navegador: `/` ✓, menu DADOS → URL `/dice` ✓, deep link `/room/NC-2020` (lobby com código preenchido + menu "JOGAR" destacado) ✓, `/sheet` e `/prd` ✓, rota inexistente → 404 + botão voltar ✓, console zero erros. Deep-link de re-join automático (recarrega e reconecta) fica para a T7.4.)*
- [x] **T7.2** Refatorar estrutura por features: `src/features/social`, `src/features/sheet`, `src/features/multiplayer`, `src/features/ai` (manter `components/ui` e `lib`). *(✓ concluído em 08/08/2026 — 14 componentes movidos com `git mv` (histórico preservado): `features/social/` = AuthModal, FriendsList, UserProfile; `features/sheet/` = DiceRoller + `CharacterSheet/` (7 arquivos); `features/multiplayer/` = MultiplayerRoom, TacticalGrid; `features/ai/` = AiAssistant. `components/` mantém apenas os compartilhados: CyberpunkMenu (nav), HomePage, PrdViewer, PresetsManager, PatchNotesFeed, ErrorBoundary e `ui/`. Todos os imports atualizados (caminhos relativos dos movidos + importadores App.tsx, SheetPage, RoomPage, CyberpunkMenu). 🐛 **Bug de build encontrado e corrigido**: rodar o esbuild sem `--packages=external` empacotava o `vite` inteiro no bundle do servidor e quebrava o `import.meta.url` (ERR_INVALID_URL ../../package.json) — o build correto é `vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap`. Validação: tsc 0 erros + build ✓ (code-splitting mantido) + navegador: `/` ✓, `/sheet` ✓ (7 módulos da ficha), `/dice` ✓, `/ai` ✓, `/multiplayer` ✓ (`GET /api/rooms` 200), `/profile` ✓, console zero erros.)*
- [x] **T7.3** Criar camada `src/api/` centralizada (cliente HTTP para `/api/rooms/*` e Gemini) em vez de fetch espalhado. *(✓ concluído em 08/08/2026 — novos módulos: `src/api/http.ts` (wrapper `apiFetch<T>` + `ApiError` com status HTTP e mensagem do servidor; falha de rede vira `ApiError(0)`), `src/api/rooms.ts` (18 endpoints tipados de `/api/rooms*`: `listRooms`, `createRoom`, `joinRoom`, `postHeartbeat`, `syncSheet`, `postMessage`, `postRoll`, `leaveRoom`, `updateTacticalGrid`, `generateNpc`, `generatePlayers`, `setPlayerHealth`, `setNpcHealth`, `deleteNpc`, `deletePlayer`, `setInitiativeList`, `nextTurn`, mais `getPeerId`/`reconnectSession`/`authedFetch` — a auto-reconexão T3.3 (401 → re-join com mesmo peerId → retry) que vivia em useCallback no componente agora é módulo), `src/api/gemini.ts` (`askGemini`) e `src/api/index.ts` (barrel). Componentes migrados: `MultiplayerRoom` (5 fetches crus + authedFetch/roomAction locais removidos → namespace `roomsApi.*`; −110 linhas) e `AiAssistant` (fetch do Gemini → `askGemini`). **Zero `fetch(` cru em src/ fora da camada api** (verificado por grep). Validação: tsc 0 erros + build ✓ + navegador: lobby com poll `listRooms` ✓, `joinRoom` na sala CIWS-6CX2 ✓, chat via WS ✓, Death Save server-authoritative ✓, `leaveRoom` ✓, `/ai` carrega ✓, console sem erros da aplicação.)*
- [x] **T7.4** Validar build e deep-links (`/room/ABC123` recarrega e reconecta). *(✓ concluído em 08/08/2026 — implementado o re-join automático no deep link: `hydrateSession()` em `src/api/rooms.ts` restaura peerId + token do sessionStorage na store no boot, e o `RoomPage` faz `joinRoom` automático quando há sessão persistida (guard por código para o StrictMode) — o servidor T3.3 reconhece o peerId e restaura a ficha, e o WS reconecta sem clique. Visitante sem sessão cai no lobby com o código preenchido; sala inexistente mostra o erro do servidor via ApiError. Validação no navegador: deep link `/room/CIWS-6CX2` (full-page) → mesa direto ✓; **reload em `/room/CIWS-6CX2` → reconectou sozinho** (view active, sem lobby) ✓; `/room/ZZ-9999` → lobby + "Room not found" ✓; console zero erros. Build ✓ + tsc 0 erros + testes 52/52.)*
- [x] ✅ **Fase 7 concluída em:** 08/08/2026

---

## FASE 8 — PRD REAL, DOCUMENTAÇÃO E IDENTIDADE DO PRODUTO  **[P1]**
**Objetivo:** o PRD do produto passa a existir de verdade (hoje `prdData.ts` está vazio) e a identidade
pública é 100% própria — **sem nenhum crédito a ferramentas de scaffold na UI, README ou HTML**.

- [x] **T8.1** Criar `docs/PRD.md`: visão, personas, escopo, funcionalidades por módulo, regras de negócio CP2020, não-objetivos, métricas. *(✓ concluído em 08/08/2026 — `docs/PRD.md` criado a partir das fontes reais (prdData.ts, README, PROTOCOLO_MULTIPLAYER.md, package.json): visão e princípios (fidelidade CP2020, servidor autoritativo, zero perda de dados, identidade própria); personas (Jogador, Mestre/GM, Netrunner/entusiasta); escopo dos 7 módulos com rotas; funcionalidades detalhadas por módulo — incluindo o estado REAL do produto (WebSocket + fallback SSE, grid Yjs CRDT, RNG server-authoritative, deep-link que reconecta, camada src/api/, Zustand, React Router com lazy); regras de negócio CP2020 (atributos 2–10, FNFF com explosão/fumble, dano por localização, death save 1d10 ≤ BODY, bio-monitor 0–10, rolagens server-authoritative); não-objetivos (sem conteúdo licenciado, netrunning completo fica P3, sem monetização na ALPHA, web-first); métricas de sucesso (ficha < 10 min, latência grid < 50ms, zero perda de dados, tsc 0 erros, suíte verde); arquitetura real com diagrama e stack do package.json; roadmap com fases concluídas 0–7.)*
- [x] **T8.2** Popular `src/data/prdData.ts` para alimentar o `PrdViewer` (seções, fases do roadmap, arquitetura). *(Populado nesta sessão — T0.10.)*
- [x] **T8.3** Reescrever `README.md` do zero: nome/identidade do produto, descrição, stack, instruções (`.env.example`, Supabase + Gemini API key, `npm run dev`), roadmap. **Sem banner, link ou crédito do scaffold de origem.** *(Reescrito nesta revisão — v1.2 do plano.)*
- [x] **T8.4** Corrigir `index.html`: título real ("Cyberpunk 2020 Sheet Builder"), meta description, `lang="pt-BR"`, favicon próprio. **Remover qualquer título/marca padrão de scaffold.** *(Corrigido nesta revisão.)*
- [x] **T8.5** Garantir que a UI (footer do `App.tsx`, modais, splash) exiba apenas a identidade "NETSHEET ENGINE" — nenhuma menção a ferramenta de origem. *(✓ concluído em 08/08/2026 — varredura exaustiva por grep em `src/` + `index.html` por `lovable|v0.dev|bolt.new|powered by|built with|made with|scaffold|stackblitz|create-t3`: **zero ocorrências** na UI. Verificados um a um os pontos citados: footer do App (`CYBERPUNK 2020 ENGINE · R. TALSORIAN GAMES · NETRUNNER AI LINK`), splash/header do menu (`NETSHEET ENGINE · CYBERPUNK 2020 · v0.4.0`), AuthModal (`AUTENTICAÇÃO NET-ACCESS`, Google/Supabase = providers legítimos), ErrorBoundary (`NETSHEET ENGINE`), NotFoundPage, HomePage (`SISTEMA.NET // NETSHEET ENGINE`). **Reforço de identidade**: título da aba agora é `NETSHEET ENGINE — Cyberpunk 2020 Sheet Builder` (antes sem a marca), meta description com a marca e **favicon próprio** (SVG inline "NE" preto/amarelo cyberpunk) — antes não havia favicon. Validação no navegador: título ✓, favicon ✓, varredura do texto visível por termos de origem = 0 ✓, AuthModal limpo ✓, console zero erros.)*
- [x] **T8.6** Documentar decisões (ADR): `docs/adr/` para Supabase, Yjs/WebSockets, Zustand, rpg-dice-roller. *(✓ concluído em 08/08/2026 — 4 ADRs criados no formato clássico (Status/Data/Contexto/Opções/Decisão/Consequências/Referências), fiéis às decisões reais: `0001-supabase.md` (Fase 2: migração Firebase → Supabase, RLS em todas as tabelas, fichas JSONB, bucket avatars, migração de dados antigos cancelada), `0002-yjs-websockets.md` (Fase 5: ws puro + Yjs no mesmo socket, SSE como fallback, RNG server-authoritative, JSON persistido como verdade durável — com a tabela de alternativas Hocuspocus vs Socket.IO vs ws), `0003-zustand.md` (Fase 4: 4 stores por domínio, fim do prop drilling, selector por slice) e `0004-dice-roller.md` (Fase 6: motor FNFF com audit trail, −157 linhas de lógica manual, 52 testes determinísticos, nota do limite client-only vs RNG server-authoritative na mesa).)*
- [x] **T8.7** Atualizar `metadata.json` e demais metadados do produto com nome/descrição finais; **remover campos de metadados específicos de plataforma de scaffold** (ex.: `majorCapabilities`) que não fazem sentido para o produto. *(✓ concluído em 08/08/2026 — `metadata.json` agora contém apenas `name: "NETSHEET ENGINE"` e `description` própria do produto; o campo `majorCapabilities` (específico da plataforma de origem) foi removido. Verificação ampla: `package.json` (name `cyberpunk-2020-suite`, sem campos de plataforma) e `components.json` (config shadcn legítima) estão ok; nenhum outro metadado de scaffold restante no repo.)*
- [x] ✅ **Fase 8 concluída em:** 08/08/2026

---

## FASE 9 — TESTES  **[P2]**
**Objetivo:** regras do jogo e fluxos críticos protegidos.

- [x] **T9.1** Configurar Vitest + React Testing Library. *(✓ concluído em 08/08/2026 — devDeps: `vitest@4.1`, `jsdom@29`, `@testing-library/react@16`, `@testing-library/jest-dom@7`, `@testing-library/user-event@14`. Infra: `vitest.config.ts` (plugin React + alias `@` + environment jsdom + setup), `src/test/setup.ts` (matchers jest-dom + cleanup automático entre testes). Scripts: `test:unit` (`vitest run`) e `test` agora roda **dice engine (52) + vitest (3)** num comando só — o CI pega tudo com `npm run test --if-present`. **Smoke tests** `src/__tests__/ui-smoke.test.tsx` (3/3): Button shadcn com variante cva, página 404 dentro do Router e navegação real com `user-event` (MemoryRouter + Routes). Nota: localmente o `npm run test` esbarra no `&` do caminho da pasta (problema conhecido T2.15 — binários invocados direto; no CI o caminho é limpo e o script funciona). Validação: tsc 0 erros (testes tipam com imports explícitos do vitest) + build ✓ + 52/52 + 3/3.)*
- [x] **T9.2** Testes unit: estatísticas derivadas (BTM, SP, perda de humanidade), dice engine, npcGenerator, regras de ferimento. *(✓ concluído em 09/08/2026 — **89/89 testes Vitest** em `src/__tests__/`. Para testar as regras sem DOM, extraí as regras puras em `src/utils/derivedStats.ts` (BTM via tabela BODY+REF, Humanidade EMP×10, perda de humanidade Σ actualHL com clamp em 0, Run/Walk MA×3, SP por localização) e `src/utils/injuryRules.ts` (níveis 0..10, penalidades REF/MA estruturadas por nível, clamp, morte iminente) — os componentes StatBlock/HealthTracker/CyberwareManager/WeaponsArmor/TacticalGrid foram refatorados para consumi-las (mudança de comportamento zero, só remoção de tabela morta no StatBlock). **dice engine migrado para Vitest** (`src/__tests__/dice-engine.test.ts`, 18 testes — explosão, explosão encadeada, fumble, dano+local, death save, faixa 4..14 com seed, determinismo); `scripts/test-dice-engine.ts` removido e `npm run test` agora é só `vitest run`. **Suítes novas**: derived-stats (32 testes com limites exatos da tabela BTM), injury-rules (27 testes — textos legados do Bio-Monitor idênticos), npc-generator (9 testes de invariantes para os 7 arquétipos). Validação: tsc 0 erros, build ✓, navegador ✓ (ficha renderiza com BTM/Humanidade/Run/Walk corretos, penalidades por nível e death save desabilitado no nível 10).)*
- [x] **T9.3** Testes de integração do servidor: criar/join/leave sala, iniciativa, GM permissions (usando supertest ou node:test). *(✓ concluído em 09/08/2026 — **51 testes de integração** em `src/__tests__/`. Para testar sem subir a porta 3000, `server.ts` agora **exporta o `app` Express** e pula `startServer()` quando `NODE_ENV=test` (Vitest define isso automaticamente; `roomPersistence` também vira no-op em test — nunca toca o Supabase mesmo com credenciais no .env.local). **`server-api.integration.test.ts` (28 testes, supertest)**: criar/join/leave sala (código inválido → 400, sala inexistente → 404), sync de ficha, **todas as GM permissions** (player-health, tactical-grid, settings, npcs generate/health/delete, players generate/delete, iniciativa — jogador → 403 com clamp do woundLevel/modifier), heartbeat (200/401), rolagem server-authoritative (attack/save/skill, tipo inválido → 400) e chat com handle derivado do servidor (anti-spoofing, vazio → 403). **`room-manager.integration.test.ts` (23 testes, direto no núcleo)**: sessões por sala (T1.7), reconexão com ficha **last-write-wins por updatedAt** (T3.3), claim de GM pelo handle, transferência de GM ao sair (T1.8), sala encerrada no último leave, timeout de isOnline (T3.4), sanitização/clamp de iniciativa e NPCs só-GM. **🐛 Bug real de produção encontrado e corrigido**: os rate limiters `roomLimiter` (120/min) e `chatLimiter` (30/min) compartilhavam um único mapa de buckets keyed por IP — o limite do chat contava TODAS as requisições da sala e derrubava o chat com 429 em mesas ativas após 30 req/min em qualquer endpoint; fix com mapa de buckets por-limiter. Validação: tsc 0 erros, 140/140 testes, build ✓, E2E WS 5/5 contra o servidor de produção rebuilt.)*
- [x] **T9.4** Testes e2e (Playwright): fluxo de auth → criar ficha → sala multiplayer (2 navegadores). *(✓ concluído em 09/08/2026 — **Playwright + Chromium** (devDep `@playwright/test`) com `playwright.config.ts` que sobe o **servidor de produção** (dist/server.cjs, NODE_ENV=production) via webServer. **6 testes E2E** em `e2e/` rodando contra o app real: (1) AuthModal abre do perfil em modo visitante com Google + e-mail/senha e alterna para cadastro; (2) gating de auth — “Criar Mesa como GM” desabilitada sem login; (3) **multiplayer com 2 navegadores** (contextos isolados = peerIds diferentes): ambos entram na mesma sala via UI, **chat realtime bidirecional** (A→B e B→A via WebSocket) e **rolagem server-authoritative** (death save pedido por A aparece no chat de B vinda do RNG do servidor) — sala semeada via REST e cleanup com leave em `finally`; (4) ficha padrão renderiza com estatísticas derivadas (BTM/Humanidade); (5) editar handle persiste após reload (localStorage modo visitante — T2.13); (6) Bio-Monitor com penalidades por nível e death save desabilitado no nível 10. Login real (Google/email) fica fora do CI por exigir credenciais do Supabase — o fluxo de UI é coberto. Script `test:e2e` (`playwright test`) + **job `e2e` no CI** (build → instala Chromium com `--with-deps` → roda os 6 testes → publica relatório HTML como artifact). Validação: 6/6 E2E local, tsc 0 erros (specs tipados — `e2e` e `playwright.config.ts` entram no include), 140/140 unit/integração.)*
- [x] **T9.5** RLS: testes de política (script de verificação em SQL com `set role` para anon/authenticated). *(✓ concluído em 09/08/2026 — a suíte `supabase/tests/rls_tests.sql` (criada na T2.19, 49/49) foi **ampliada para 56/56** e **automatizada**: agora é **auto-seed** (cria os usuários A/B/C em `auth.users` com `on conflict do nothing` — o trigger `handle_new_user` gera os perfis) e ganhou o runner **`scripts/test-rls.mjs`** (descobre o container `supabase_db_*` via `docker ps` com override `SUPABASE_DB_CONTAINER`, pipeia o SQL no psql, parseia o resumo `pass|fail|total` e sai com exit code 0/1). Cobertura confirmada no schema atual (0001–0006): **anon bloqueado** em profiles/friendships/friend_requests/direct_messages/character_sheets/rooms (insert denied, select 0, update/delete sem policy) exceto leitura pública de avatares e buckets; **authenticated cross-user bloqueado** (update/delete de perfil alheio, amizade não-participada, solicitação como outro remetente, mensagem em chat alheio ou como outro remetente, ficha de terceiro, upload/alterar/deletar objeto na pasta de outro — DELETE direto em storage também barrado pelo trigger `protect_delete`); **rooms servidor-only** (RLS sem policies — nem anon nem authenticated). Validado: `node scripts/test-rls.mjs` → **56/56 PASS** (exit 0) contra o Supabase Docker local. Nota: os testes exigem o Postgres Supabase real (schema auth/storage/RLS) — rodam localmente, não no CI do GitHub.)*
- [x] ✅ **Fase 9 concluída em:** 09/08/2026

---

## FASE 10 — DEPLOY, CI/CD E HARDENING DE PRODUÇÃO  **[P1]**
**Objetivo:** aplicação acessível publicamente com pipeline automatizado e produção endurecida.

- [x] **T10.1** Backend (Express + WebSocket): deploy em Railway/Fly.io/Render (processo Node, variáveis `GEMINI_API_KEY`, `VITE_SUPABASE_*`, porta). *(✓ concluído em 09/08/2026 — o backend está **pronto para deploy em 1 processo** (API + WS + SPA estática). **`server.ts`**: porta/host agora vêm do ambiente (`PORT`/`HOST`, default 3000/0.0.0.0 — as plataformas injetam PORT) e `/api/*` desconhecida responde **JSON 404** (não HTML da SPA). **`Dockerfile` multi-stage** (node 22-alpine): build com `npm ci` + `npm run build` (VITE_* como build-args inline no bundle do cliente), runtime só com deps de produção + `dist/` (vite é dependência regular e o bundle do servidor externaliza pacotes), `HEALTHCHECK` nativo em `/api/health`. **`.dockerignore`** (exclui node_modules/dist/.git/segredos/e2e). **Configs de plataforma**: `render.yaml` (runtime Node nativo, plano free, healthcheck `/api/health`, env com `sync: false`), `fly.toml` (Docker build + secrets + healthcheck + 1 máquina/1GB), `railway.toml` (Nixpacks + start + healthcheck). **`.env.example`** documenta build-time (VITE_SUPABASE_URL/ANON_KEY) vs runtime (SUPABASE_URL/SERVICE_ROLE_KEY, GEMINI_API_KEY, PORT). **`docs/DEPLOY.md`**: arquitetura, tabela de env, passo a passo das 3 plataformas, verificação pós-deploy e nota operacional de **1 instância** (estado das salas é em memória — WS ao vivo por instância; persistência Supabase restaura no boot). `package.json`: `engines.node >= 20` + `lib0` declarado (o server.ts o importa diretamente). **Validação**: tsc 0 erros · 140/140 testes · build ✓ · PORT env (3456) → health 200 · JSON 404 ✓ · **Docker build + run → health 200, SPA 200, E2E WS 5/5 dentro do container** ✓.)*
- [ ] **T10.2** Frontend: build estático (Vite) no Vercel/Netlify com env vars do cliente.
- [ ] **T10.3** Configurar domínio customizado (opcional) e HTTPS.
- [ ] **T10.4** Healthcheck `/api/health` monitorado (uptime bot).
- [ ] **T10.5** CI/CD: GitHub Actions (ou equivalente) — `tsc --noEmit`, lint, testes, build em cada PR/push.
- [ ] **T10.6** Hardening do servidor: CORS restrito (hoje não há middleware CORS), `helmet` (security headers), rate limiting global, logs estruturados.
- [ ] **T10.7** Checklist de produção: secrets fora do bundle (nunca `VITE_` com segredo de service role), variáveis de ambiente por ambiente, revisão de RLS em produção.
- [ ] **T10.8** Banco de dados: ativar **backups automáticos/PITR** do Supabase e aplicar migrations via `supabase db push` no pipeline de CI/CD.
- [ ] ✅ **Fase 10 concluída em:** ____/____/______

---

## FASE 11 — REGRAS CP2020 AVANÇADAS  **[P3]**
**Objetivo:** profundidade de sistema.

- [ ] **T11.1** Inventário e peso (ENC/EV) com encumbrance automático.
- [ ] **T11.2** Netrunning completo (MU, programas, data walls, ações por turno).
- [ ] **T11.3** Pós-ferimento automático: aplicar penalidades de REF por estado de ferimento (Sério -2 etc.).
- [ ] **T11.4** Loot/NPCs: gerenciador de inventário de NPCs e drops.
- [ ] **T11.5** Export/Import de ficha (JSON + PDF print).
- [ ] ✅ **Fase 11 concluída em:** ____/____/______

---

## FASE 12 — VALIDAÇÃO FINAL E ENCERRAMENTO DO PLANO  **[P0]**
**Objetivo:** garantir que tudo está concluído e **deletar este arquivo**.

- [ ] **T12.1** `git status` limpo (sem pendências); todos os commits com mensagens claras.
- [ ] **T12.2** Rodar suíte completa: `tsc --noEmit`, `npm run build`, testes unit/integration/e2e, lint.
- [ ] **T12.3** Teste manual completo em produção (staging): auth, social, ficha, mesa multiplayer com 2+ usuários reais.
- [ ] **T12.4** Conferir que **nenhum checkbox acima ficou sem `[x]`** e que todas as datas de fase estão preenchidas.
- [ ] **T12.5** Arquivar resumo do roadmap concluído no `README.md`.
- [ ] **T12.6** 🗑️ **DELETAR ESTE ARQUIVO** (`git rm PLANO_DE_ACAO.md` + commit "Plano de ação concluído — remove documento mestre").
- [ ] ✅ **Plano integralmente concluído em:** ____/____/______

---

## 📊 RESUMO DE PROGRESSO (preencher a cada sessão)

| Fase | Descrição | Status | Data |
|------|-----------|--------|------|
| 0 | Fundação e recuperação do código | ✅ concluída | 03/08/2026 |
| 1 | Correções de segurança | ✅ concluída | 03/08/2026 |
| 2 | Migração Firebase → Supabase | ✅ concluída (T2.1–T2.20; T2.9 cancelada) | 07/08/2026 |
| 3 | Multiplayer: persistência | ✅ concluída (T3.1–T3.5) | 08/08/2026 |
| 4 | Estado frontend (Zustand) | ⬜ | — |
| 5 | Multiplayer real-time (WS/Yjs) | ⬜ | — |
| 6 | Motor de dados | ⬜ | — |
| 7 | Roteamento e estrutura | ⬜ | — |
| 8 | PRD, documentação e identidade | ⬜ | — |
| 9 | Testes | ⬜ | — |
| 10 | Deploy, CI/CD e hardening | ⬜ | — |
| 11 | Regras CP2020 avançadas | ⬜ | — |
| 12 | Validação final + delete deste arquivo | ⬜ | — |

> 🔒 **Incidente de segurança resolvido em 08/08/2026 (pós-Fase 3):** o GitHub Secret Scanning detectou a **Web API Key do Firebase antigo** (`AIzaSy[REDIGIDO]`, projeto `concise-waters-p1ttq`) commitada na Fase 0 (`src/lib/firebase.ts`, removido na T2.15) e ainda presente no HEAD em `docs/legacy/firebase-applet-config.json`. Correção completa: (1) chave substituída por placeholder `REMOVIDA-SEGURANCA` no HEAD (commit `e618832`); (2) **história reescrita** com `git filter-branch` — todos os 23 commits purgados, refs originais + reflog + GC eliminados, zero ocorrências em qualquer blob; (3) force push + alerta GitHub marcado como **resolved/revoked**. Verificado: o **Google OAuth Client Secret** (GOCSPX-) nunca vazou (só em `supabase/.env` gitignored); chaves `service_role` nunca vazaram. ⚠️ **Ação pendente do usuário:** revogar/rotacionar a API Key no Google Cloud Console (APIs & Services → Credentials) — o projeto Firebase antigo pode ser excluído. Repo foi **criado em 08/08/2026** (público, `tavinholoco/NetsheetEngine`), CI GitHub Actions verde (tsc + build + smoke em Node 20/22).

**Última atualização:** 08/08/2026 — **revisão v1.17**: Fases 0, 1, 2, 3 e 4 concluídas; **Fase 5 em andamento — T5.1, T5.2, T5.3 e T5.4 concluídas** (T5.3: **Yjs v13 no TacticalGrid** — grid tático como **estado CRDT** sobre o MESMO WebSocket (handler `y-protocols`: sync incremental + awareness), `src/lib/gridDoc.ts` (doc↔grid puro) + `src/lib/yjsConnection.ts` (cliente) + doc/awareness por sala no servidor com espelho autoritativo doc↔JSON (GM tudo; jogador move o próprio token; mutação não autorizada revertida sem chegar aos outros), seed JSON→doc nas mutações REST, cursores do GM via awareness renderizados no grid, e **jogador pode arrastar o próprio token**; validado 12/12 E2E + navegador. Decisões T5.1/T5.2 mantidas: WebSocket puro como transporte base, SSE fallback, JSON como verdade durável). T5.2: **WebSocket puro `ws` implementado** em `/ws/rooms/:code` no mesmo Express — handshake com token T1.7 (close 4401), mensagens JSON chat/heartbeat/initiative, broadcast unificado WS+SSE com persistência (T3.1); cliente com **fallback automático SSE** e envio WS-first com fallback POST; validado 11/11 E2E + navegador). Decisão T5.1 documentada: **WebSocket puro `ws` como transporte base** para chat/rolls/iniciativa — zero deps novas, reconexão via T3.3, SSE como fallback — + **Yjs v13 só no TacticalGrid** como CRDT para tokens e awareness de cursores, integrado ao mesmo WebSocket sem Hocuspocus separado; **RNG server-authoritative** na T5.4; Socket.IO rejeitado). Fase 4 entregue: **estado global com Zustand** (`zustand@^5`) em `src/stores/` — `useSheetStore` (bridge com `useCharacterSheet`: persistência cloud/local/autosave preservada), `useRoomStore` (sala multiplayer reativa), `useRollStore` (histórico/banner + broadcast), `useUiStore` (abas, auth modal, toasts) — eliminando o prop drilling do `App.tsx` (CyberpunkMenu, DiceRoller, PresetsManager, UserProfile e MultiplayerRoom leem das stores; props reduzidas a callbacks). Decisão de arquitetura: **bridge em vez de reescrever a persistência** da ficha para não arriscar regressão na T2.13; `syncSheetStore` espelha o hook na store a cada render. Também em 08/08/2026: **gitleaks integrado ao CI** (varredura de segredos em todo push/PR + SARIF na aba Security) e incidente da Firebase API Key resolvido e documentado.
Fase 2 (progresso 2.1–2.8/2.10–2.16): ambiente local **rodando** (Docker Desktop instalado; `supabase start` no ar:
API `127.0.0.1:54321`, Studio `54323`, Postgres `54322`); migrations **0001** (schema + RLS + triggers),
**0002** (Realtime p/ `direct_messages`/`profiles`/`friend_requests` + bucket `avatars` com RLS de pasta)
e **0003** (listagem pública de buckets) e **0004** (remoção de `profiles.email` — privacidade, pois o Realtime
não filtra colunas, apenas linhas; e-mail próprio fica no JWT) aplicadas; `.env.local` preenchido com chaves
locais; Email/Senha validado com teste funcional (signup → perfil `#NC-####` → upload avatar → cross-user
bloqueado: 10/10 ✓; pós-reset 7/7 ✓).
**✅ Fase 2 concluída em 07/08/2026** (T2.1–T2.8, T2.10–T2.20; T2.9 cancelada — sem migração de dados, banco limpo; Firebase totalmente removido; Supabase é o único backend).

> **✅ T2.11 — Google OAuth validado (07/08/2026):** após corrigir o Client ID para `774463152952-74orl6td...apps.googleusercontent.com` (faltava o prefixo do nº do projeto), o **login real com conta Google funcionou de ponta a ponta** no navegador — sessão criada no Supabase local e perfil sincronizado. Client ID em `supabase/config.toml` (público, versionado); Client Secret em `supabase/.env` (gitignored, nunca commitar).
>
> **Passo a passo original (para referência):**
> 1. Acessar [console.cloud.google.com](https://console.cloud.google.com) → criar/selecionar projeto.
> 2. **APIs & Services → OAuth consent screen**: escolher External, preencher app name (ex.: NETSHEET ENGINE)
>    e e-mail; adicionar o escopo `.../auth/userinfo.email` e `.../auth/userinfo.profile`; salvar.
> 3. **APIs & Services → Credentials → Create credentials → OAuth client ID** → tipo **Web application**.
>    Authorized redirect URIs: `http://127.0.0.1:54321/auth/v1/callback` (local).
> 4. Copiar o **Client ID** e o **Client secret**.
> 5. No `supabase/config.toml`: `[auth.external.google] enabled = true`, `client_id = "<seu-client-id>"` e
>    `secret = env(SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET)` (criar a env var antes de rodar `supabase start`),
>    OU preencher via Studio local (`http://127.0.0.1:54323` → Authentication → Providers → Google).
> 6. Reiniciar o stack: `supabase stop && supabase start`.
> Total: 13 fases, ~100 tarefas (83 pendentes).

> 🔁 **Regra de ouro:** ao iniciar qualquer sessão, comece por Fase 0 e siga em ordem.
> Se uma tarefa for concluída, marque `[x]` e atualize a tabela de resumo. Nunca deixe tarefas
> concluídas desmarcadas. Quando Fase 12 for atingida, **delete este arquivo**.
